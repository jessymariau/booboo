---
"create-booboo": patch
"@booboo-brain/cli": patch
---

A one-line GitHub star ask at the two entry commands, placed where the product has just proved itself: the end of the `create-booboo` scaffold output, and `booboo view` in `--demo` mode only — daily snapshot users are never nagged.
